import { test, expect } from '../../fixtures/fixtures';
import systData from '../../test-data/syst-customer-portal.json';

test('TCP-20-04', async ({ customerPortalLoggedIn, page }) => {
  const dataArray = (systData as any)['TCP-20-04'] as any[];

  
});
