{
  "name": "gatiglobe-playwright-tests",
  "version": "1.0.0",
  "description": "Customer Portal automation — data-driven TCP-20-0* via syst-customer-portal.json, all methods in CustomerPortalPage (readonly getByRole), 1 worker headed",
  "scripts": {
    "test": "playwright test tests/customer-portal --project=chromium --workers=1",
    "test:customer": "playwright test tests/customer-portal --project=chromium --workers=1",
    "test:headed": "playwright test tests/customer-portal --project=chromium --headed --workers=1",
    "test:customer:headed": "playwright test tests/customer-portal --project=chromium --headed --workers=1",
    "test:ui": "playwright test tests/customer-portal --project=chromium --ui --workers=1",
    "test:debug": "playwright test tests/customer-portal --project=chromium --debug --workers=1",
    "report": "playwright show-report",
    "codegen": "playwright codegen https://www.gatiglobe.in/login"
  },
  "keywords": ["playwright", "e2e", "automation", "gatiglobe", "erp"],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.14.0",
    "dotenv": "^16.4.5"
  }
}
